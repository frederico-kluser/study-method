# Modelos e binários de voz (onda 8) — GITIGNORED (são staged, não commitados).
# Mantidos versionados: resources/licenses/**, resources/.gitkeep e
# resources/tracks/.gitkeep — este último mantém `resources/tracks/` EXISTINDO
# e vazio. O diretório sumiu do git em 2026-09-02, quando as trilhas geradas
# foram apagadas (ver docs/15-trilha-nodejs.md); sem o .gitkeep o
# `listTrackSlugs` do loader levaria ENOENT num clone limpo e a Home mostraria
# "falha ao listar trilhas" em vez de uma lista vazia.

# STT (Nemotron via sherpa-onnx-node) — ~683 MB
stt-models/

# TTS (Piper — engine binário s/ plataforma + modelos + dados espeak-ng GPLv3)
tts-engine/
tts-models/
espeak-ng-data/