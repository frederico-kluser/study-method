{
  "schemaVersion": 1,
  "slug": "query-parametrizada-simulada",
  "title": "Query parametrizada simulada: o valor nunca vira comando",
  "concept": "sql_injection",
  "difficulty": 5,
  "language": "nodejs",
  "statement": "# Query parametrizada simulada: o valor nunca vira comando\n\nNa aula você viu que o certo é passar valores por parâmetros (`?`), nunca colar texto digitado no SQL. Aqui você vai simular o que o banco faz com esses parâmetros: escrever `montarQuerySegura(template, valores)`, que substitui cada `?` do template pelo valor JÁ ESCAPADO.\n\n## Regras de escape (como o SQLite faz)\n\n- **texto** — vai entre aspas simples, e cada `'` interno é DOBRADO: `Ana` → `'Ana'`; `O'Brien` → `'O''Brien'`;\n- **número** — vai sem aspas: `42` → `42`;\n- **booleano** — `true` → `1`; `false` → `0`;\n- **null ou undefined** — viram a palavra `NULL`;\n- **outros tipos** (lista, objeto, função) — lance um erro com \"tipo\" na mensagem.\n\n## Regras do comando\n\n- os valores substituem os `?` NA ORDEM em que aparecem no template;\n- se a quantidade de `?` não bater com a quantidade de valores, lance um erro com \"marcadores\" na mensagem.\n\n## Exemplo\n\n```js\nmontarQuerySegura('SELECT * FROM usuarios WHERE email = ?', ['ana@clinica.com']);\n// \"SELECT * FROM usuarios WHERE email = 'ana@clinica.com'\"\n\nmontarQuerySegura('SELECT * FROM usuarios WHERE nome = ?', [\"O'Brien\"]);\n// \"SELECT * FROM usuarios WHERE nome = 'O''Brien'\"   ← a aspas dobrada\n```\n\nSe o atacante digitar `' OR '1'='1`, o resultado vira uma busca por um nome LITERAL — com todas as aspas dobradas dentro de um único par — e nenhum comando novo é criado.\n\nDica: conte os `?` com `template.match(/\\?/g)` (ou `split`); substitua um de cada vez com `template.replace('?', ...)` — o replace com texto troca só a PRIMEIRA ocorrência.\n\nLeia o enunciado com calma e clique em Começar para iniciar o cronômetro.",
  "starterCode": "// Simule uma query parametrizada: substitua cada ? pelos valores escapados.\nexport function montarQuerySegura(template, valores) {\n  throw new Error('não implementado');\n}\n",
  "testsCode": "import { test } from 'node:test';\nimport assert from 'node:assert/strict';\nimport { montarQuerySegura } from './solution.mjs';\n\ntest('texto simples vira texto entre aspas simples', () => {\n  assert.equal(\n    montarQuerySegura('SELECT * FROM usuarios WHERE email = ?', ['ana@clinica.com']),\n    \"SELECT * FROM usuarios WHERE email = 'ana@clinica.com'\",\n  );\n});\n\ntest(\"aspas do usuário são dobradas — o comando não muda de sentido\", () => {\n  assert.equal(\n    montarQuerySegura('SELECT * FROM usuarios WHERE nome = ?', [\"O'Brien\"]),\n    \"SELECT * FROM usuarios WHERE nome = 'O''Brien'\",\n  );\n  // a tentativa de injeção inteira vira um ÚNICO texto literal\n  assert.equal(\n    montarQuerySegura('SELECT * FROM usuarios WHERE nome = ?', [\"' OR '1'='1\"]),\n    \"SELECT * FROM usuarios WHERE nome = ''' OR ''1''=''1'\",\n  );\n});\n\ntest('número fica sem aspas, booleano vira 1/0 e null vira NULL', () => {\n  assert.equal(\n    montarQuerySegura('SELECT * FROM consultas WHERE id = ?', [42]),\n    'SELECT * FROM consultas WHERE id = 42',\n  );\n  assert.equal(\n    montarQuerySegura('SELECT * FROM consultas WHERE confirmada = ?', [true]),\n    'SELECT * FROM consultas WHERE confirmada = 1',\n  );\n  assert.equal(\n    montarQuerySegura('SELECT * FROM consultas WHERE confirmada = ?', [false]),\n    'SELECT * FROM consultas WHERE confirmada = 0',\n  );\n  assert.equal(\n    montarQuerySegura('SELECT * FROM consultas WHERE telefone = ?', [null]),\n    'SELECT * FROM consultas WHERE telefone = NULL',\n  );\n});\n\ntest('aplica vários valores na ordem e reclama quando a contagem não bate', () => {\n  assert.equal(\n    montarQuerySegura(\"SELECT * FROM usuarios WHERE email = ? AND senha = ?\", ['a@b.com', '123']),\n    \"SELECT * FROM usuarios WHERE email = 'a@b.com' AND senha = '123'\",\n  );\n  assert.throws(\n    () => montarQuerySegura('SELECT * FROM usuarios WHERE email = ? AND senha = ?', ['a@b.com']),\n    /marcadores/,\n  );\n  assert.throws(\n    () => montarQuerySegura('SELECT * FROM usuarios WHERE nome = ?', [['Ana']]),\n    /tipo/,\n  );\n});\n",
  "solutionCode": "// Query parametrizada simulada: o template fica fixo e cada ? recebe um\n// valor JÁ ESCAPADO — é o que o banco faz com os parâmetros de verdade.\nexport function montarQuerySegura(template, valores) {\n  if (typeof template !== 'string') {\n    throw new Error('template precisa ser texto');\n  }\n  if (!Array.isArray(valores)) {\n    throw new Error('valores precisa ser uma lista');\n  }\n  const marcadores = (template.match(/\\?/g) ?? []).length;\n  if (marcadores !== valores.length) {\n    throw new Error(\n      `o template tem ${marcadores} marcadores e foram passados ${valores.length} valores`,\n    );\n  }\n\n  let sql = template;\n  for (const valor of valores) {\n    sql = sql.replace('?', escaparValor(valor)); // troca só o PRIMEIRO ?\n  }\n  return sql;\n}\n\n// Escapa UM valor do jeito do SQLite: texto entre aspas com ' dobrada,\n// número puro, booleano vira 1/0, null vira NULL, resto é erro.\nfunction escaparValor(valor) {\n  if (valor === null || valor === undefined) return 'NULL';\n  if (typeof valor === 'number') return String(valor);\n  if (typeof valor === 'boolean') return valor ? '1' : '0';\n  if (typeof valor === 'string') {\n    return `'${valor.replaceAll(\"'\", \"''\")}'`;\n  }\n  throw new Error('tipo de valor não suportado');\n}\n",
  "expectedTestCount": 4
}
