// Fixture da coleta recursiva do tools/t.sh — extensão .test.tsx.
import { test } from 'node:test';
import assert from 'node:assert/strict';

test('t-sh coletou d.test.tsx (extensão tsx)', () => {
  assert.ok(true);
});
