{
  "schemaVersion": 1,
  "slug": "let-e-atribuicao",
  "title": "Let e atribuição",
  "summary": "que let declara uma variável mutável (com valor inicial) e que a atribuição (=) substitui o valor guardado",
  "difficulty": 1,
  "concepts": [
    "let_e_atribuicao"
  ],
  "prerequisites": [],
  "theory": [
    {
      "id": "predicao",
      "title": "Predição",
      "markdown": "Antes de escrever qualquer coisa nesta trilha, uma leitura. O harness testa as funções que você exporta: ele as importa, as chama e compara o que cada uma devolve com um valor esperado. Esse formato já é seu — ele aparece em toda tarefa —, então a primeira interação de hoje é a mesma de sempre: **prever** o que o teste vê, sem rodar nada.\n\nEsta é a função que o harness vai ler:\n\n```js\nexport function x() {\n  return 1;\n}\n```\n\nE este é o teste que ele roda sobre ela:\n\n```js\nimport { test } from 'node:test';\nimport assert from 'node:assert/strict';\nimport { x } from './solution.mjs';\n\ntest('a previsão', () => {\n  assert.equal(x(), 1);\n});\n```\n\n**Preveja** (não conta para nada — é só para você conferir o próprio modelo mental): quando o teste roda, o que `x()` devolve? O `assert.equal(x(), 1)` está comparando o quê? O teste passa ou falha?\n\nA execução confronta a previsão: `x()` devolve **1** — o valor que a função devolve — e o teste **passa**, porque 1 é exatamente o valor esperado do lado direito. Repare no desenho: o teste compara o que a função devolve com um valor fixo. Esta aula transforma essa mesma pergunta em habilidade de escrita: quando você declarar uma variável, o que importa é **qual valor ela guarda no momento em que é usada**."
    },
    {
      "id": "modelo-mental",
      "title": "Modelo mental",
      "markdown": "O termo técnico desta aula é **variável**: um nome dentro do programa que guarda **um valor por vez**. Criar a variável é **declarar**; trocar o valor que ela guarda é **atribuir**; usar o nome em um lugar onde um valor é esperado é **ler** o valor.\n\nUma analogia concreta: pense numa **etiqueta de identificação**. Você escreve um nome na etiqueta (por exemplo, `contador`) e a prende em um único objeto por vez. Para etiquetar outro objeto, você desprende e prende em outro — o objeto anterior não fica guardado junto; ele é **substituído**. É exatamente isso que a variável faz: o nome (`contador`) é a etiqueta; o **valor** é o objeto em que ela está presa; `let` **declara** a etiqueta (cria o nome), e `=` **atribui** (troca em qual valor a etiqueta está presa agora).\n\nReempacotando — a mesma analogia, aplicada linha a linha no código:\n\n```js\nexport function marcar() {\n  // declarar: criar a etiqueta `contador` presa no valor 0\n  let contador = 0;\n  // atribuir: trocar a etiqueta para o valor 5 — o 0 é substituído\n  contador = 5;\n  // ler-valor: devolver o nome devolve o valor preso agora: 5\n  return contador;\n}\n```\n\nA linha 1 **declara** a variável com `let` e já a deixa nascer com o primeiro valor (0). A linha 2 **atribui** um valor novo (5): o 0 é substituído, não somado nem guardado ao lado. A linha 3 **lê** o valor atual e o devolve: 5.\n\nOnde a analogia quebra: a etiqueta **não segura a expressão**, segura o **resultado**. `contador = 5` não guarda \"a conta 5\"; guarda o valor 5. E a etiqueta não \"lembra\" do valor anterior: depois da linha 2, o 0 não existe mais na variável — foi substituído pelo 5."
    },
    {
      "id": "exemplo-numero",
      "title": "Exemplo número",
      "markdown": "Um **worked example**: um programa sendo construído em pequenos incrementos que rodam. O contexto desta vez é numérico — um placar.\n\nIncremento 1 — declarar e ler:\n\n```js\nexport function placar() {\n  // declarar: criar a variável `pontos` presa no valor inicial 0\n  let pontos = 0;\n  // ler-valor: devolver pontos devolve o valor atual (0)\n  return pontos;\n}\n```\n\nRoda. O teste `assert.equal(placar(), 0)` passa: ler o nome `pontos` devolve o valor inicial, 0. Nenhum erro.\n\nIncremento 2 — atribuir troca o valor guardado:\n\n```js\nexport function placar() {\n  // declarar: criar a variável `pontos` presa no valor inicial 0\n  let pontos = 0;\n  // atribuir: trocar a etiqueta para o valor 10 — o 0 é substituído\n  pontos = 10;\n  // ler-valor: agora ler pontos devolve 10\n  return pontos;\n}\n```\n\nRoda. O teste `assert.equal(placar(), 10)` passa: a atribuição (`=`) **substituiu** o 0 pelo 10. Se a variável acumulasse valores, o teste ainda esperaria 10 (0 + 10) — a diferença entre substituir e acumular só aparece quando o valor inicial não é zero; por isso o próximo incremento muda o valor inicial.\n\nIncremento 3 — a diferença fica visível:\n\n```js\nexport function placar() {\n  // declarar: a variável `pontos` nasce presa no valor 3\n  let pontos = 3;\n  // atribuir: o valor guardado vira 10 — o 3 é substituído\n  pontos = 10;\n  // ler-valor: ler pontos devolve 10\n  return pontos;\n}\n```\n\nRoda. O teste `assert.equal(placar(), 10)` passa. Se atribuir somasse, `pontos` guardaria 13 (3 + 10); guardou **10**: o 3 foi substituído, não somado.\n\n**Qual é o estado agora?** No incremento 3, depois da linha `let pontos = 3;`, o que está guardado em `pontos`? E depois da linha `pontos = 10;`?\n\nResposta: depois da linha 1, `pontos` guarda **3**; depois da linha 2, guarda **10** — a última atribuição vence, e o valor anterior deixou de existir na variável."
    },
    {
      "id": "exemplo-string",
      "title": "Exemplo string",
      "markdown": "Segundo **worked example** — a segunda forma sintática da declaração. Valor não é só número: texto também é valor, e `let` declara a variável do mesmo jeito, agora com aspas simples.\n\nIncremento 1 — declarar e ler:\n\n```js\nexport function cumprimentar() {\n  // declarar: criar a variável `nome` presa no valor inicial 'ana'\n  let nome = 'ana';\n  // ler-valor: devolver o nome devolve o texto guardado\n  return nome;\n}\n```\n\nRoda. O teste `assert.equal(cumprimentar(), 'ana')` passa: texto entre aspas é um valor, e ler o nome devolve exatamente o texto guardado.\n\nIncremento 2 — atribuir com texto:\n\n```js\nexport function cumprimentar() {\n  // declarar: criar a variável `nome` presa no valor inicial 'ana'\n  let nome = 'ana';\n  // atribuir: trocar a etiqueta para o texto 'bia' — 'ana' é substituído\n  nome = 'bia';\n  // ler-valor: agora o nome devolve 'bia'\n  return nome;\n}\n```\n\nRoda. O teste `assert.equal(cumprimentar(), 'bia')` passa: a atribuição trocou o valor guardado também com texto — 'ana' saiu, 'bia' entrou.\n\n**Qual é o estado agora?** Depois de `let nome = 'ana';`, o que `nome` guarda? E depois de `nome = 'bia';`?\n\nResposta: **'ana'** e depois **'bia'** — a mesma regra do número, aplicada a texto."
    },
    {
      "id": "refutacoes",
      "title": "Refutações",
      "markdown": "Três leituras enganosas aparecem com frequência quando se aprende variável. Cada uma tem o par errado/certo ancorado na especificação da linguagem.\n\n**1. \"A variável é uma caixa que vai acumulando: cada atribuição soma ao que já estava lá.\"** Não. A atribuição avalia o lado direito, vira um valor e **sobrescreve** o que a variável guardava — é o passo PutValue da especificação (ECMA-262, 13.15.4; MDN, seção Assignment).\n\n```js\nlet total = 10;\ntotal = 3;\n```\n\nLeitura errada: \"total guarda 13 (10 + 3)\". Leitura certa: total guarda **3** — o 10 foi sobrescrito. Atribuir não é acrescentar; é trocar.\n\n**2. \"Para trocar o valor, eu redeclaro.\"** Não. O par errado — redeclarar o mesmo nome em `let` é erro de sintaxe (SyntaxError): a especificação proíbe que a mesma declaração nomeie a variável duas vezes no mesmo lugar (ECMA-262, 14.3.2; MDN, seção Redeclarations):\n\n```js\nlet x = 1;\nlet x = 2;\n```\n\nO par certo — declarar uma vez e **atribuir** a segunda:\n\n```js\nlet x = 1;\nx = 2;\n```\n\n**3. \"Posso atribuir sem declarar: funciona.\"** Não. Neste curso o código roda como módulo e módulos rodam em strict mode: atribuir a um nome que nunca foi declarado lança ReferenceError na execução (ECMA-262, 13.15.4; MDN, seção Strict mode). O par errado:\n\n```js\nx = 1;\n```\n\nO par certo — primeiro **declarar**, depois **atribuir**:\n\n```js\nlet x = 0;\nx = 1;\n```"
    },
    {
      "id": "referencia",
      "title": "Referência just-in-time — a sintaxe, colada ao desafio desta aula.",
      "markdown": "Referência just-in-time — a sintaxe, colada ao desafio desta aula.\n\n| O que você quer | Como escrever |\n| --- | --- |\n| declarar (criar) a variável já com um valor inicial | `let contador = 0;` |\n| trocar o valor guardado (atribuir) | `contador = 5;` |\n| usar o valor atual (ler) | usar o nome no lugar em que um valor é esperado |\n\n```js\n// declarar — criar a variável com o valor inicial\nlet pontos = 0;\n// atribuir — trocar o valor guardado (o antigo é substituído)\npontos = 5;\n```\n\nNo desafio desta aula a lacuna é a **declaração**: você escreve a linha `let ... = ...;` com o valor inicial. A linha de atribuição já vem pronta e congelada no código — em relação a ela, a sua tarefa é **ler** e saber que ela troca o valor guardado. Escrever atribuição sozinha fica para a próxima aula; ficam para outras aulas também: `const`, `var`, escopo de bloco, operadores compostos como `+=`, incremento `++` e várias declarações na mesma linha.\n\nTermos desta aula: **variável**, **declaração**, **atribuição**, **valor**."
    },
    {
      "id": "drill",
      "title": "Drill",
      "markdown": "Um item curto de treino (opcional). Leia o código e responda **qual é o estado agora** — o que está guardado em `valor` depois de cada linha — sem rodar:\n\n```js\nlet valor = 7;\nvalor = 2;\n```\n\nResposta: depois da linha 1, `valor` guarda **7**; depois da linha 2, guarda **2** — a segunda atribuição substituiu a primeira. Se atribuição acumulasse, seria 9; não é: ela substitui (ECMA-262, 13.15.4).\n\n**Checksum de cauda.** Nesta aula você aprendeu a **usar**: `let` (declarar uma variável com valor inicial). Você **leu**: `=` (atribuição — substituir o valor guardado). Os termos da aula: **variável**, **declaração**, **atribuição**, **valor**. Nenhuma outra construção apareceu em nenhum exemplo de código — a lista fecha com o orçamento da aula."
    }
  ],
  "sources": [
    {
      "title": "Ecma262",
      "url": "https://tc39.es/ecma262/#sec-assignment-operators",
      "description": "a atribuição avalia o lado direito e chama PutValue sobre a referência, que SOBRESCREVE o valor da binding (misconception 1 e 3)"
    },
    {
      "title": "Ecma262",
      "url": "https://tc39.es/ecma262/#sec-let-and-const-declarations",
      "description": "BoundNames duplicados de let no mesmo escopo é SyntaxError (misconception 2)"
    },
    {
      "title": "Assignment",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Assignment",
      "description": "MDN sobre atribuição"
    },
    {
      "title": "Let",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/let#redeclarations",
      "description": "MDN sobre redeclaração de let"
    },
    {
      "title": "Strict mode",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Strict_mode#assigning_to_undeclared_variables",
      "description": "MDN sobre atribuir a variável não declarada em strict mode"
    }
  ],
  "challenges": [
    "contador-com-let"
  ],
  "objective": {
    "verbo": "demonstrar",
    "enunciado": "que let declara uma variável mutável (com valor inicial) e que a atribuição (=) substitui o valor guardado",
    "contexto": "primeira aula da trilha: o aluno ainda não conhece nenhuma construção de JavaScript além das que o harness (export function e node:test) já lhe apresenta como leitura obrigatória",
    "criterio": "o aluno declara uma variável com let e valor inicial no span de uma lacuna única; a atribuição (=) é apresentada nos worked examples e congelada no starter como leitura (receptiva), e o teste valida que o valor final produzido é o da última atribuição"
  },
  "introduces": {
    "productive": [
      "decl:let"
    ],
    "receptive": [
      "op:assign:=",
      "node:VariableStatement",
      "node:VariableDeclaration",
      "node:VariableDeclarationList",
      "node:BinaryExpression",
      "node:NumericLiteral",
      "node:StringLiteral",
      "node:FunctionDeclaration",
      "node:ReturnStatement",
      "node:ExportKeyword"
    ]
  },
  "introducesTerms": [
    "variável",
    "declaração",
    "atribuição",
    "valor"
  ],
  "foraDeEscopo": [
    "const (decl:const) — construção-irmã que proíbe reatribuição (SyntaxError); exige consolidar declarar+atribuir antes — aula própria.",
    "var (decl:var) — escopo de função e hoisting; semântica histórica — aula própria ou fora do currículo.",
    "escopo de bloco e zona morta temporal — onde a variável vive é outro aspecto da máquina nocional — aula própria.",
    "operadores compostos de atribuição (+=) e incremento/decremento (++) — combinam leitura+operação+escrita num só operador — aula própria.",
    "múltiplos declaradores na mesma linha (let a = 1, b = 2;) — forma composta do VariableDeclarationList — estoura o span de uma lacuna única.",
    "cenário de erro (throw / assert.throws) — A11: o orçamento não contém op:throw nem api:assert.throws; o primeiro átomo não trabalha com erros."
  ],
  "eiClass": "regra",
  "role": "regular",
  "targetAtom": "decl:let",
  "notionalMachineDelta": "Uma variável guarda UM valor por vez, e a atribuição TROCA o valor guardado: o lado direito da atribuição é avaliado primeiro (vira um valor) e só então esse valor SUBSTITUI o antigo no espaço da variável — nada é somado, acumulado nem guardado em paralelo. Declarar com let cria o espaço já com o primeiro valor; a partir daí, ler o nome da variável devolve o valor mais recente.",
  "budgetHash": "experimento-sem-LLM",
  "budgetVersion": "experimento-v1",
  "status": "rascunho",
  "research": [
    "https://tc39.es/ecma262/#sec-assignment-operators — a atribuição avalia o lado direito e chama PutValue sobre a referência, que SOBRESCREVE o valor da binding (misconception 1 e 3)",
    "https://tc39.es/ecma262/#sec-let-and-const-declarations — BoundNames duplicados de let no mesmo escopo é SyntaxError (misconception 2)",
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Assignment — MDN sobre atribuição",
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/let#redeclarations — MDN sobre redeclaração de let",
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Strict_mode#assigning_to_undeclared_variables — MDN sobre atribuir a variável não declarada em strict mode"
  ]
}
