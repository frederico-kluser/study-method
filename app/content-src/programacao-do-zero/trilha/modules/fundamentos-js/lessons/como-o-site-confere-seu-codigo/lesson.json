{
  "schemaVersion": 1,
  "slug": "como-o-site-confere-seu-codigo",
  "title": "Como o site confere seu código",
  "summary": "que o site confere o código por meio de uma máquina que importa a caixa exportada, a chama e compara o valor devolvido com o esperado (assert.equal), e que o invólucro (export, function, chaves, return) é leitura congelada",
  "difficulty": 1,
  "concepts": [
    "como_o_site_confere_seu_codigo"
  ],
  "prerequisites": [],
  "theory": [
    {
      "id": "como-ler-o-desafio",
      "title": "Como ler o desafio",
      "markdown": "Esta é a primeira coisa que você vai ver em quase todas as atividades desta trilha: um **invólucro** — a moldura de código que já vem pronta, esperando só uma parte sua. Antes de escrever qualquer coisa, vamos aprender a **ler** esse invólucro. Ele não é uma pergunta: é leitura, e será explicado aqui.\n\nEsta é a moldura do desafio de hoje:\n\n```js\nexport function resposta() {\n  return /* ESCREVA UM NÚMERO AQUI */;\n}\n```\n\nLendo peça por peça (é só leitura — você não escreve nada disto):\n\n- `export` — a palavra que **entrega** a caixa ao conferidor; sem ela, o site não enxerga a caixa.\n- `function resposta()` — a **caixa** de instruções, com o nome `resposta`.\n- `{ ... }` — as **chaves** que guardam as instruções da caixa.\n- `return` — a palavra que **devolve** um valor para fora da caixa.\n- `/* ESCREVA UM NÚMERO AQUI */` — a **lacuna**: o único lugar que é seu nesta atividade. O texto entre `/*` e `*/` é um comentário — um aviso que a máquina ignora; você vai apagá-lo e escrever um **número** no lugar.\n\nAgora veja a mesma moldura preenchida:\n\n```js\nexport function resposta() {\n  return 7;\n}\n```\n\nQuando a lacuna tem o número `7`, a caixa `resposta` devolve o `7`. O número `7` é um **valor**: a coisa que a caixa entrega.\n\nE como o site confere esse número? Ele roda uma **máquina que confere**: um **teste**. O teste é a pergunta da máquina — \"o que `resposta()` devolve? deve ser igual ao esperado\". A comparação é feita pelo **assert**: a ferramenta que **afirma** que o valor devolvido é o esperado — e, se não for, mostra a diferença.\n\n```js\nassert.equal(resposta(), 7);\n```\n\nLendo essa linha: `resposta()` **chama** a caixa (roda as instruções dela) e `assert.equal(...)` compara o valor devolvido com o `7` do lado direito. Iguais? O teste passa. Diferentes? A máquina responde algo como *esperava 7, recebeu ...*.\n\nConfira você mesmo, só lendo: na moldura preenchida acima, que número a caixa devolve quando é chamada? Resposta: o `7` — o valor que está na lacuna."
    },
    {
      "id": "a-maquina-que-confere",
      "title": "A maquina que confere",
      "markdown": "Por trás do site, a máquina que confere faz três passos, nesta ordem: **importa** a sua caixa, **chama** a caixa e **compara** o valor devolvido com o esperado. Este é o arquivo de teste completo — é ele que roda quando você clica em conferir:\n\n```js\nimport { test } from 'node:test';\nimport assert from 'node:assert/strict';\nimport { resposta } from './solution.mjs';\n\ntest('escolha o número 7', () => {\n  assert.equal(resposta(), 7);\n});\n```\n\nLendo (só leitura — este arquivo não é seu):\n\n- As três primeiras linhas **importam** (buscam) as peças: o `test` (a ferramenta que anuncia a pergunta), o `assert` (a ferramenta que compara) e a sua caixa `resposta`, que vive no arquivo da solução.\n- `test('escolha o número 7', () => { ... })` — declara a pergunta: a caixa `resposta` deve devolver **7**. O texto entre aspas é só o nome da pergunta.\n- Dentro, `assert.equal(resposta(), 7)` — a comparação em si: chama a caixa e compara o valor devolvido com o `7`.\n\nE se a lacuna tiver outro número — digamos `5`?\n\n```js\nexport function resposta() {\n  return 5;\n}\n```\n\nO teste continua esperando `7`. A caixa devolve `5`; a máquina compara `5` com `7` — não são iguais — e responde algo como *esperava 7, recebeu 5*. **Nenhum número é aceito: nesta atividade, o único valor que passa é o 7** — exatamente o esperado do teste.\n\nTrês leituras enganosas comuns:\n\n1. **\"Basta escrever qualquer número.\"** Não. A comparação é com o valor esperado do teste. Com `5` na lacuna, a caixa devolve `5`, e `5` não é `7`.\n2. **\"A máquina lê o meu arquivo como texto.\"** Não. A máquina **roda** o código: ela chama `resposta()` e compara o **valor devolvido**, não as palavras do arquivo.\n3. **\"Eu preciso mexer no invólucro também.\"** Não. O invólucro está **congelado**: `export`, `function`, as chaves e o `return` já estão prontos, e você não os escreve nesta atividade. A lacuna é o único lugar seu — e é onde mora a parte da conferência."
    },
    {
      "id": "referencia",
      "title": "Referência just-in-time — o essencial colado ao desafio desta aula.",
      "markdown": "Referência just-in-time — o essencial colado ao desafio desta aula.\n\n| O que você vê | O que é | Você faz? |\n| --- | --- | --- |\n| `export` | entrega a caixa ao conferidor | não — congelado |\n| `function resposta()` | a caixa de instruções (nome `resposta`) | não — congelado |\n| `{ ... }` | as instruções da caixa | não — congelado |\n| `return` | devolve o valor para fora | não — congelado |\n| a lacuna `/* ... */` | o único lugar seu | **sim — escreva um número** |\n\nA moldura do desafio:\n\n```js\nexport function resposta() {\n  return /* ESCREVA UM NÚMERO AQUI */;\n}\n```\n\nApague `/* ESCREVA UM NÚMERO AQUI */` e escreva **um número** no lugar — o número que a máquina deve conferir. Nesta atividade a máquina espera o **7**: o teste compara o que `resposta()` devolve com o `7`.\n\nPronto fica assim:\n\n```js\nexport function resposta() {\n  return 7;\n}\n```\n\nFicam para outras aulas: escrever o invólucro (export, function, chaves, return), escrever a chamada, escrever o próprio teste, textos e outros tipos de valor. Hoje o seu único ato é digitar o número `7`.\n\nTermos desta aula: **invólucro**, **lacuna**, **máquina que confere**, **teste**, **assert**."
    },
    {
      "id": "drill",
      "title": "Um item curto de treino (opcional) — só leitura, sem rodar nada:",
      "markdown": "Um item curto de treino (opcional) — só leitura, sem rodar nada:\n\n```js\nexport function resposta() {\n  return 3;\n}\n```\n\nO teste desta aula espera `7`. Se a máquina rodar essa caixa, ela devolve `3`; `3` é diferente de `7`. O teste passa ou falha?\n\nResposta: **falha** — a mensagem da máquina é algo como *esperava 7, recebeu 3*. Só o número `7` passa neste teste.\n\n**Checksum de cauda.** Nesta aula você **leu** o invólucro (export, function, chaves, return) e a máquina que confere (teste e assert), e **escreveu** um número — um valor — na lacuna. Nenhuma outra construção apareceu em nenhum exemplo de código: a lista fecha com o orçamento da aula."
    }
  ],
  "sources": [
    {
      "title": "Export",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/export",
      "description": "export: a palavra que entrega a caixa ao importador (o conferidor)"
    },
    {
      "title": "Return",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/return",
      "description": "return devolve o valor para quem chamou a caixa"
    },
    {
      "title": "Functions",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions",
      "description": "declaração de função como caixa nomeada de instruções"
    },
    {
      "title": "Test",
      "url": "https://nodejs.org/api/test.html",
      "description": "node:test: a função test() anuncia a pergunta que a máquina roda"
    },
    {
      "title": "Assert",
      "url": "https://nodejs.org/api/assert.html",
      "description": "assert.equal: a comparação entre o valor devolvido e o esperado"
    },
    {
      "title": "Strict equality",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Strict_equality",
      "description": "a igualdade estrita usada na conferência (7 === 7)"
    }
  ],
  "challenges": [
    "digite-o-numero-7"
  ],
  "objective": {
    "verbo": "demonstrar",
    "enunciado": "que o site confere o código por meio de uma máquina que importa a caixa exportada, a chama e compara o valor devolvido com o esperado (assert.equal), e que o invólucro (export, function, chaves, return) é leitura congelada",
    "contexto": "primeira aula da trilha: o aluno ainda não conhece nenhuma construção de JavaScript; o invólucro e o teste aparecem como LEITURA explicada, nunca interrogados como conhecimento prévio (feedback do usuário: a 1ª atividade não pode presumir função/parâmetro)",
    "criterio": "o aluno digita um número (7) na lacuna única do invólucro congelado `export function resposta() { return /* lacuna */; }` e o teste `assert.equal(resposta(), 7)` passa — o invólucro permanece byte a byte congelado entre starter e solução"
  },
  "introduces": {
    "receptive": [
      "node:ExportKeyword",
      "node:FunctionDeclaration",
      "node:Identifier",
      "node:Block",
      "node:ReturnStatement",
      "node:NumericLiteral",
      "node:ExpressionStatement",
      "node:CallExpression",
      "node:PropertyAccessExpression",
      "node:StringLiteral",
      "api:assert.equal",
      "api:node:test",
      "api:node:assert/strict",
      "node:ImportDeclaration",
      "node:ImportSpecifier",
      "node:ImportClause",
      "node:NamedImports",
      "node:ArrowFunction"
    ],
    "productive": [
      "node:NumericLiteral"
    ]
  },
  "introducesTerms": [
    "invólucro",
    "lacuna",
    "máquina que confere",
    "teste",
    "assert"
  ],
  "foraDeEscopo": [
    "escrever qualquer parte do invólucro (export, function, chaves, return) — o curriculo o congela; escrevê-lo produtivamente é aula própria (involucro-completo, mais adiante na trilha)",
    "escrever a CHAMADA resposta() — aparece só como leitura (na linha do assert e no teste); a chamada produtiva é aula própria (funcao-e-chamada)",
    "parâmetro e argumento — entram na assinatura da caixa; aula própria (parametro-e-argumento)",
    "erro de sintaxe (a máquina não entendeu) e mensagens de exceção — fora do orçamento; aula própria (erro-sintaxe-vs-erro-valor)",
    "outros tipos de valor (texto, verdadeiro/falso) — o único valor desta aula é o número; aula própria (string-como-valor e seguintes)",
    "console.log / impressão na tela — global:console fora do orçamento; o canal de conferência desta trilha é o retorno da função"
  ],
  "eiClass": "regra",
  "role": "regular",
  "targetAtom": "node:NumericLiteral",
  "notionalMachineDelta": "A máquina que confere roda o arquivo do aluno como módulo, importa a função exportada, CHAMA a caixa (resposta()) e compara o valor devolvido com o valor esperado do teste (assert.equal — igualdade estrita). Se iguais, o teste passa; se diferentes, a mensagem mostra o esperado e o recebido ('esperava 7, recebeu ...'). O invólucro (export, function, chaves, return) é congelado no starter: a única parte que o aluno muda é o valor dentro do return. O número digitado é um VALOR; a linha return é a instrução que o entrega (tema da aula 2).",
  "budgetHash": "programacao-do-zero-v1",
  "budgetVersion": "v1",
  "status": "rascunho",
  "research": [
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/export — export: a palavra que entrega a caixa ao importador (o conferidor)",
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/return — return devolve o valor para quem chamou a caixa",
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions — declaração de função como caixa nomeada de instruções",
    "https://nodejs.org/api/test.html — node:test: a função test() anuncia a pergunta que a máquina roda",
    "https://nodejs.org/api/assert.html — assert.equal: a comparação entre o valor devolvido e o esperado",
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Strict_equality — a igualdade estrita usada na conferência (7 === 7)"
  ]
}
