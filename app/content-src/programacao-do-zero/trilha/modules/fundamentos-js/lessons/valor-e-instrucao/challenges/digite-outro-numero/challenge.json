{
  "schemaVersion": 1,
  "slug": "digite-outro-numero",
  "title": "Digite outro numero",
  "concept": "valor_e_instrucao",
  "difficulty": 1,
  "language": "nodejs",
  "statement": "# Digite outro número: 42\n\nValor é o que o programa entrega; instrução é a linha que roda.\n\nVocê já conhece a máquina que confere (da aula 1): ela roda a caixa `resposta()`, recebe o número devolvido e compara com o esperado. Agora o esperado é **42**.\n\nO número que você digita é um **valor** — a coisa que o programa entrega. E a linha `return ...;` (já pronta e congelada) é a **instrução** — a linha que roda e entrega esse valor à máquina.\n\nApague `/* DIGITE OUTRO NÚMERO AQUI */` e escreva **42** no lugar. Só o número — nada mais.",
  "starterCode": "export function resposta() {\n  return /* DIGITE OUTRO NÚMERO AQUI */;\n}\n",
  "testsCode": "import { test } from 'node:test';\nimport assert from 'node:assert/strict';\nimport { resposta } from './solution.mjs';\n\ntest('o número conferido é 42', () => {\n  assert.equal(resposta(), 42);\n});\n",
  "solutionCode": "export function resposta() {\n  return 42;\n}\n",
  "expectedTestCount": 1,
  "outputChannel": "retorno",
  "requires": [
    "valor-e-instrucao (node:NumericLiteral — reuso do número em contexto de instrução)",
    "como-o-site-confere-seu-codigo (leitura do invólucro e da máquina que confere — aula 1)"
  ],
  "requirements": [
    {
      "id": "REQ-1",
      "descricao": "A função resposta() devolve o número 42 — o valor exato que a máquina conferidora espera nesta atividade (nenhum outro número passa).",
      "teste": "o número conferido é 42"
    }
  ],
  "notRequired": [
    "reexplicar o invólucro (export, function, chaves, return) — leitura da aula 1; aqui só se relembra a máquina",
    "escrever a linha return — congelada; a instrução que entrega já está pronta",
    "escolher outro número — a resposta desta atividade é fixa: o teste espera o 42",
    "qualquer construção nova além do número — esta é uma aula de reuso"
  ],
  "subgoals": [
    "digitar outro número",
    "ler a instrução congelada (return ...;)",
    "prever o valor que a máquina recebe"
  ],
  "scenarios": [
    {
      "tipo": "exemplo",
      "derivado_de": "node:NumericLiteral",
      "descricao": "a lacuna preenchida com o número 42 — a solução de referência; a instrução return entrega o valor 42 e o teste passa"
    },
    {
      "tipo": "valido",
      "derivado_de": "node:NumericLiteral",
      "descricao": "o 42 é o único valor que passa: assert.equal(resposta(), 42) compara com o valor exato esperado"
    },
    {
      "tipo": "limite",
      "derivado_de": "node:NumericLiteral",
      "descricao": "43 ou qualquer outro número devolvem um valor diferente do esperado — o teste falha com 'esperava 42, recebeu ...' (a leitura enganosa 'valor e instrução são a mesma coisa' não muda o resultado)"
    },
    {
      "tipo": "invalido",
      "derivado_de": "node:NumericLiteral",
      "descricao": "lacuna vazia (return sem número) devolve undefined — o teste falha com 'esperava 42, recebeu undefined'"
    }
  ],
  "taskSkill": "digitar um número (o 42) no span da lacuna única do mesmo invólucro congelado, reconhecendo o número como valor e a linha return ...; como instrução — reuso de node:NumericLiteral",
  "supportLevel": "com_andaime",
  "surfaceDomain": "conceito de valor × instrução; escrita de um literal numérico (reuso) dentro do invólucro congelado",
  "solutionAlternates": [],
  "wrongSolutions": [
    "export function resposta() {\n  return 43;\n}\n",
    "export function resposta() {\n  return ;\n}\n"
  ]
}
