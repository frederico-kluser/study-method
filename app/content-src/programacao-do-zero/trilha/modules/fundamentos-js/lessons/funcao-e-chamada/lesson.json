{
  "schemaVersion": 1,
  "slug": "funcao-e-chamada",
  "title": "Função e chamada",
  "summary": "que uma função é uma caixa nomeada de instruções que devolve um valor, e que CHAMAR a caixa é escrever o nome dela com parênteses",
  "difficulty": 2,
  "concepts": [
    "funcao_e_chamada"
  ],
  "prerequisites": [
    "como-o-site-confere-seu-codigo"
  ],
  "theory": [
    {
      "id": "a-caixa-e-o-botao",
      "title": "A caixa e o botao",
      "markdown": "Nas aulas 1 e 2 você leu a moldura que o site congela em toda tarefa: uma caixa nomeada com uma linha de devolução. Ela continua congelada hoje — o que muda é o que você faz COM a caixa: **chamar**.\n\n**Função** é o nome técnico da caixa: um bloco nomeado de instruções que **devolve um valor**. **Chamar** é pedir esse valor: escrever o nome da caixa seguido de **parênteses**. A chamada **roda** a caixa (executa as instruções dela) e o valor devolvido aparece no lugar da chamada.\n\n```js\n// A caixa, congelada (você só lê — da aula 1 para cá ela não mudou):\nfunction resposta() {\n  return 5;\n}\n\n// Chamar a caixa é escrever o nome com parênteses:\nexport function conferidor() {\n  return resposta();\n}\n```\n\nRepare na última linha: `return resposta();` devolve **o valor da chamada**. `resposta()` roda a caixa `resposta` e o resultado — o número 5 — é exatamente o que `conferidor()` devolve quando o site a chama. A frase toda desta aula cabe numa linha: **para pedir o valor de uma caixa, escreva o nome dela com parênteses**."
    },
    {
      "id": "maquina-e-botao",
      "title": "Maquina e botao",
      "markdown": "Uma analogia que ajuda: pense numa **máquina de devolver valor** — como uma daquelas máquinas de refrigerante onde o que importa é o que sai. A **função** é a máquina: ela fica ali, parada, com um valor pronto dentro. **Chamar** a função é **pedir o valor apertando o botão**: os **parênteses** são o botão. Enquanto ninguém aperta, a máquina não faz nada; quando você escreve o nome com os parênteses, a máquina roda e o valor sai no lugar do botão.\n\nMapeando a analogia no código:\n\n```js\n// A máquina, definida — parada, com o valor 5 dentro:\nfunction resposta() {\n  return 5;\n}\n\n// Aperta o botão: a máquina roda e o 5 aparece aqui:\nexport function conferidor() {\n  return resposta();\n}\n```\n\nA mesma máquina pode ser acionada mais de uma vez — cada chamada roda a caixa de novo e devolve o mesmo valor, porque a caixa não guarda nada que mude entre uma chamada e outra:\n\n```js\nfunction resposta() {\n  return 5;\n}\n\nexport function primeiro() {\n  return resposta();\n}\n\nexport function segundo() {\n  return resposta();\n}\n```\n\n`primeiro()` devolve 5 e `segundo()` devolve 5 — cada um apertou o botão da mesma máquina. Onde a analogia quebra: a máquina não 'transmite' nada — ela **devolve** o valor para quem chamou, e é esse valor que o teste compara. Nenhuma caixa deste curso recebe nada pelos parênteses ainda — eles ficam **vazios**. Escrever algo entre os parênteses é assunto de uma aula futura."
    },
    {
      "id": "o-que-acontece-na-tela",
      "title": "O que acontece na tela",
      "markdown": "Quando você aperta **Conferir**, o site faz exatamente o que você acabou de ver: ele chama a caixa entregue e compara o valor devolvido com um valor esperado. O arquivo de teste que o site roda é este — leia, não escreva:\n\n```js\nimport { test } from 'node:test';\nimport assert from 'node:assert/strict';\nimport { conferidor } from './solution.mjs';\n\ntest('a caixa chamada devolve 5', () => {\n  assert.equal(conferidor(), 5);\n});\n```\n\nA linha que interessa é `assert.equal(conferidor(), 5)`: o site **chama** `conferidor()` e compara o que veio com o número 5. Como `conferidor()` devolve `resposta()` — e `resposta()` devolve 5 — o valor bate e o teste passa. Se a sua lacuna tivesse `resposta` **sem** os parênteses, `conferidor()` devolveria a **caixa parada** (a máquina inteira), não o valor 5 — e o teste compararia uma máquina com o número 5: não bate. Os parênteses são a diferença entre pedir o valor e apontar para a máquina."
    },
    {
      "id": "referencia",
      "title": "Referência just-in-time — o que você precisa para o desafio desta aula.",
      "markdown": "Referência just-in-time — o que você precisa para o desafio desta aula.\n\n| O que você quer | Como escrever |\n| --- | --- |\n| chamar a caixa `resposta` | `resposta()` |\n| chamar e devolver o valor na mesma linha | `return resposta();` |\n\n```js\n// Chamar: nome da caixa + parênteses. A caixa roda e o valor aparece aqui.\nreturn resposta();\n```\n\nNo desafio, a caixa `resposta` já está pronta e congelada — você só a **chama**. A lacuna é uma linha só: `resposta()`, o nome com os parênteses. Ficam para outras aulas: escrever a declaração da caixa, escrever a linha `return`, escrever algo dentro dos parênteses (a caixa não recebe nada ainda) e a palavra `export`. Termos desta aula: **função**, **chamada**, **definir**, **rodar**, **parênteses**."
    },
    {
      "id": "drill",
      "title": "Um item curto de treino (opcional) — leia o código e **preveja** o valor antes de qualquer coisa:",
      "markdown": "Um item curto de treino (opcional) — leia o código e **preveja** o valor antes de qualquer coisa:\n\n```js\nfunction resposta() {\n  return 5;\n}\n\nexport function conferidor() {\n  return resposta();\n}\n```\n\nPreveja: o que `conferidor()` devolve? E se a lacuna fosse `resposta` **sem** os parênteses — o que a máquina responderia?\n\nResposta: `conferidor()` devolve **5** — a chamada roda a caixa e o valor devolvido aparece no lugar da chamada. Sem os parênteses, a lacuna devolveria a **caixa inteira** (a função parada), não o valor 5 — e o teste, que espera o número 5, não bateria.\n\n**Checksum de cauda.** Nesta aula você aprendeu a **usar**: a **chamada** de função — `nome()` — para pedir o valor de uma caixa. Você **leu**: a caixa (definida com `function` e uma linha que devolve) e o teste do site, que chama a caixa entregue e compara o valor devolvido. Os termos da aula: **função**, **chamada**, **definir**, **rodar**, **parênteses**. Nenhuma outra construção apareceu em nenhum exemplo de código — a lista fecha com o orçamento da aula."
    }
  ],
  "sources": [
    {
      "title": "Functions",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions",
      "description": "funções como blocos reutilizáveis de código, chamadas com ( )"
    },
    {
      "title": "Functions",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions",
      "description": "referência de funções e da sintaxe de chamada"
    },
    {
      "title": "Ecma262",
      "url": "https://tc39.es/ecma262/#sec-function-definitions",
      "description": "semântica de definição de função e da avaliação da expressão de chamada (CallExpression)"
    }
  ],
  "challenges": [
    "chamar-a-caixa"
  ],
  "objective": {
    "verbo": "demonstrar",
    "enunciado": "que uma função é uma caixa nomeada de instruções que devolve um valor, e que CHAMAR a caixa é escrever o nome dela com parênteses",
    "contexto": "aulas 1-2 já leram o invólucro export/function/return como moldura congelada; o aluno sabe que o site chama funções no teste, mas nunca escreveu uma chamada",
    "criterio": "o aluno escreve a chamada sem argumentos (`resposta()`) no span de uma lacuna única; a declaração de função permanece congelada e receptiva; o teste valida que o valor devolvido é o da caixa chamada (5)"
  },
  "introduces": {
    "receptive": [
      "node:FunctionDeclaration"
    ],
    "productive": [
      "node:CallExpression"
    ]
  },
  "introducesTerms": [
    "função",
    "chamada",
    "definir",
    "rodar",
    "parênteses"
  ],
  "foraDeEscopo": [
    "escrever a declaração de função — o aluno só a escreve na aula do invólucro completo, quando o return também for seu (node:FunctionDeclaration produtivo é aula própria)",
    "parâmetros e argumentos — escrever algo entre os parênteses é a aula de parâmetro; aqui a caixa não recebe nada ainda (o termo nem é introduzido)",
    "escrever a linha return — permanece congelada; a linha de devolução escrita é a aula do return",
    "console.log e impressão — a entrega é por retorno (outputChannel retorno)",
    "export escrito — a palavra export é a próxima aula; aqui a caixa entregue vem congelada"
  ],
  "eiClass": "regra",
  "role": "regular",
  "targetAtom": "node:CallExpression",
  "notionalMachineDelta": "Uma função é uma caixa nomeada de instruções que devolve um valor: a caixa existe desde que é definida (o nome + as instruções + a linha de devolução), mas nada roda até alguém CHAMAR. Chamar é escrever o nome da caixa seguido de parênteses — `resposta()`. A chamada roda a caixa do começo ao fim e o valor devolvido aparece no lugar da chamada: quem lê o código vê `resposta()`, quem roda vê o valor 5. Sem os parênteses o nome sozinho é só a caixa parada — não roda, não devolve, e não serve como valor esperado pelo teste.",
  "budgetHash": "programacao-do-zero-v1",
  "budgetVersion": "v1",
  "status": "rascunho",
  "research": [
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions — funções como blocos reutilizáveis de código, chamadas com ( )",
    "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions — referência de funções e da sintaxe de chamada",
    "https://tc39.es/ecma262/#sec-function-definitions — semântica de definição de função e da avaliação da expressão de chamada (CallExpression)"
  ],
  "assertions": [
    {
      "id": "chamar-com-parenteses",
      "sectionId": "a-caixa-e-o-botao",
      "statement": "Uma função é uma caixa nomeada de instruções que devolve um valor; chamar é escrever o nome da caixa com parênteses.",
      "question": "Para pedir o valor da caixa `resposta`, o que você escreve?",
      "options": [
        "`resposta` — só o nome",
        "`function resposta` — a palavra function antes do nome",
        "`resposta()` — o nome com parênteses",
        "`return resposta` — a palavra return antes do nome"
      ],
      "answerIndex": 2,
      "feedback": "Os parênteses são o botão: `resposta()` roda a caixa e o valor dela aparece no lugar da chamada."
    },
    {
      "id": "valor-aparece-na-chamada",
      "sectionId": "a-caixa-e-o-botao",
      "statement": "A chamada roda a caixa e o valor devolvido aparece no lugar da chamada.",
      "question": "Com `function resposta() { return 5; }`, o que devolve `conferidor()`, que tem `return resposta();`?",
      "options": [
        "a caixa inteira `resposta`",
        "o texto `resposta()`",
        "nada, porque `resposta()` não devolve valor",
        "5, o valor que a caixa `resposta` devolve"
      ],
      "answerIndex": 3,
      "feedback": "A chamada `resposta()` roda a caixa e o 5 aparece no lugar dela; `conferidor()` devolve esse 5."
    },
    {
      "id": "sem-parenteses-caixa-parada",
      "sectionId": "o-que-acontece-na-tela",
      "statement": "Sem os parênteses, o nome sozinho é a caixa parada — não o valor — e o teste não bate.",
      "question": "Se a lacuna tivesse `resposta` sem os parênteses, o que `conferidor()` devolveria?",
      "options": [
        "o número 5, do mesmo jeito",
        "a caixa inteira (a função parada), não o número 5",
        "um erro de sintaxe",
        "o número 0"
      ],
      "answerIndex": 1,
      "feedback": "Sem os parênteses você aponta para a máquina em vez de apertar o botão; o teste compararia a função parada com o 5 — não bate."
    }
  ]
}
