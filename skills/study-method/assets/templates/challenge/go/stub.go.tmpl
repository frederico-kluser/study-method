package {{PKG}}

// {{SIGNATURE}} usa retorno NOMEADO de propósito: um `panic()` aqui derrubaria o binário de
// teste inteiro e esconderia os cenários depois do primeiro — exatamente o defeito do assert.h
// em C (docs/05-challenges-tdd.md §3.2). Um `return` nu só COMPILA com resultado nomeado, e
// devolve o zero-value do tipo, que é sempre uma resposta errada: falha o teste sem abortar o
// processo. Com resultado anônimo (`) int64`) este mesmo `return` nem compilaria.
{{SIGNATURE}} {
	// SM_CORPO_INICIO — escreva a sua implementação entre estas duas marcas
	// implemente {{FUNC_NAME}} para o teste passar
	return
	// SM_CORPO_FIM
}
