// tests/test_stub.rs — a especificação executável deste desafio. Leia; não edite.
// Teste de integração: roda contra a API pública do crate `{{CRATE}}` (layout cargo_crate,
// docs/05-challenges-tdd.md §2.3). As funções #[test] ficam no TOPO deste arquivo, sem
// `mod tests`: o cargo então reporta cada caso pelo nome CURTO (`test <cenario> ... ok`), e é
// esse nome que o `meta.json` grava em `scenarios[].test_name`. Ao filtrar um caso só, use
// `-- --exact` — sem ele o filtro do cargo é por substring e casa mais do que se pediu.
use {{CRATE}}::{{FUNC_NAME}};

{{SCENARIOS_CODE}}
