// tests/test_stub.rs — a especificação executável deste desafio. Leia; não edite.
// Teste de integração: roda contra a API pública do crate `{{CRATE}}` (layout cargo_crate,
// docs/05-challenges-tdd.md §2.3) — nunca filtre por nome curto (`cargo test {{FUNC_NAME}}`
// sozinho casa 0 testes e sai 0); o `meta.json` grava o nome qualificado que o runner reporta.
use {{CRATE}}::{{FUNC_NAME}};

{{SCENARIOS_CODE}}
