# {{SETUP_NAME}}

<!--
  Este é o README.md do setup — o nó do grafo deste projeto de estudo (docs/00-contratos.md §3.5).

  As 8 seções abaixo, cada uma entre `<!-- study-method:begin <secao> -->` e
  `<!-- study-method:end <secao> -->`, são reescritas automaticamente por `readme-sync.sh` a cada
  fechamento de sessão. Não vale a pena editar o INTERIOR delas à mão: a próxima sincronização
  substitui exatamente o que está entre os marcadores, byte a byte.

  TUDO O QUE ESTIVER FORA DOS MARCADORES é seu e fica intocado para sempre — inclusive este
  comentário. Escreva à vontade acima, entre uma seção e outra, ou no final: objetivos, prazos,
  links, o que quiser. `readme-sync.sh` nunca move nem apaga uma linha que não esteja dentro de um
  par `begin`/`end`.
-->

`setup_id`: `{{SETUP_ID}}` · assunto: `{{SUBJECT}}` · criado em {{CREATED_AT}}

<!-- study-method:begin identidade -->
_Ainda sem sessão fechada — esta seção nasce vazia e é preenchida no primeiro fechamento de
sessão, por `readme-sync.sh`._
<!-- study-method:end identidade -->

<!-- study-method:begin taxonomia -->
_Nenhum tópico registrado ainda. Esta seção lista, do mais geral ao mais específico, os tópicos
que `setup.json` → `taxonomy` já cobre._
<!-- study-method:end taxonomia -->

<!-- study-method:begin base-teorica -->
_Sem base teórica indexada ainda. Quando houver material em `docs/` (deste setup) ou em
`docs/generated/`, esta seção resume o que foi lido e o que ficou de fora._
<!-- study-method:end base-teorica -->

<!-- study-method:begin destilados -->
_Nenhuma pesquisa destilada ainda. Esta seção lista os destilados de `researchs/`._
<!-- study-method:end destilados -->

<!-- study-method:begin desafios -->
_Nenhum desafio criado ainda. Esta seção lista os desafios de `challenges/` com o
`challenge_status` de cada um._
<!-- study-method:end desafios -->

<!-- study-method:begin linha-do-tempo -->
_Nenhuma sessão registrada ainda. Esta seção resume, em ordem cronológica, o `one_line_summary`
de cada sessão fechada em `memory/`._
<!-- study-method:end linha-do-tempo -->

<!-- study-method:begin pontes -->
_Nenhuma leitura cruzada com outro setup ainda. Esta seção lista as pontes registradas em
`cross_setup_refs` de alguma sessão — a razão de cada uma, em uma frase._
<!-- study-method:end pontes -->

<!-- study-method:begin estado-atual -->
_Setup recém-criado; nada para retomar ainda. Esta seção resume onde a última sessão parou e o
que ficou pendente._
<!-- study-method:end estado-atual -->

<!-- Suas próprias anotações começam aqui embaixo — este espaço nunca é tocado pela skill. -->
